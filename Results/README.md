# Experiment Results

This directory contains JSON metrics and loss curves from controlled MiniLLM
training experiments.

Each JSON file records the model configuration, training configuration,
parameter count, runtime, best validation loss, and loss history.